# Changelog

每次发布都在此记录面向用户的新增、修改和修复；最新版本置顶。

## 0.6.5

- 回退未发布的0.7拍摄预设、专业参数和视觉效果链，恢复0.6.4相机交互、普通曝光调节及照片/视频输出路径。
- 分类管理的更多菜单新增“重命名分类”；保存时同步重命名当前授权根目录中的对应文件夹，同名、无效名称、目录授权失效或存在待处理媒体时拒绝修改。
- 数据库升级到版本4：从已安装0.7测试版升级时移除自定义预设表，保留分类、拍摄队列和其他设置；应用版本为0.6.5／33，支持直接覆盖0.7.0／32。
- 单元测试、Lint和Debug构建通过；小米真机从0.7.0／32覆盖安装后成功迁移Room 3→4，7个原分类全部保留，5项数据库与分类仪器测试通过。

## 0.6.4

- 最低系统要求扩展至Android 10（API 29），支持分类、SAF归档、CameraX普通拍照和独立录像。
- Android 10～12默认使用普通拍照；点击Live开关明确说明当前连续Live后端需要Android 13的传感器时间基准接口。不会静默输出时间线不可靠的Live，也不会覆盖已保存的Live偏好。
- Android 13及以上保留原有Live流程、编码配置和音画同步算法；Camera2高版本API添加系统版本保护，Debug Live探针也明确拒绝旧系统。
- 真机UI测试加入旧系统分支，检查Live保持关闭且不改写原偏好；版本0.6.4／31。
- vivo Z5原厂Android 10验证普通JPEG（8000×6000）与有声短MP4（1080×1920）保存、完整读回及像素/视频帧解码通过；59项单元测试、6项真机测试通过，Lint无错误。0.6.4尚未在小米上重新回归。

## 0.6.3

- 首页分类名下改为该分类文件夹的照片和视频总数；Live单文件计1个，不递归计入子目录，也不计隐藏文件、音频或附属文件。
- 返回首页、分类变更与保存队列变化时立即统计；首页可见期间每3秒复查，后台和取景页停止轮询。SAF使用批量游标读取名称与MIME，避免逐文件读取元数据；读取失败明确显示“暂无法统计”。
- 删除文件夹图标下的“文件”文字，图标垂直居中；移除Lenshelf下方注释并将品牌字号调整至26sp。
- Live开关从相机设置菜单移到齿轮左侧：采用点状外环、内圆及关闭斜线，加入240ms颜色、旋转和斜线切换动画。录像时禁用开关，保留原有麦克风权限与Live后端逻辑。
- 版本更新为0.6.3／30；README可发布文件快照更新为111个。

## 0.6.2

- 分类管理页新增“导入”：扫描当前授权根目录的一级文件夹，只创建缺失分类；同名分类无论启用或停用均保持原状态并跳过。
- 导入过程不移动、复制或删除任何目录和媒体；普通文件、隐藏目录、重复目录和不合法名称会被忽略，并在完成提示中报告数量。
- Xiaomi 17 Pro 真机 SAF 集成测试读取8个现有一级目录，成功导入4个缺失分类；立即重复导入时新增数为0，目录内容保持原位。
- 删除分类默认只删除数据库中的分类记录并保留文件夹；用户明确勾选后，会删除对应分类文件夹及其中全部文件和子目录，弹窗标明永久删除且不可恢复。
- 工程发布结构重新整理：正式领域词汇移入 `docs/DOMAIN_MODEL.md`，原始真机证据、临时分析和对话衔接记录不进入公开源码仓库。
- README 增加可发布文件数量、目录用途和 GitHub 提交边界；版本更新为0.6.2／29。

## 0.6.1

- 修复 Live 首次启动时倍率列表不完整、点按对焦框坐标偏移、切应用或打开分类目录后预览冻结，以及高分辨率 JPEG 请求导致动态画面白闪的问题。
- 将拍摄、排队和预热状态统一为顶部紧凑提示，减少对取景区域的遮挡。
- 音频采集改为按每次 `AudioRecord` 硬件时间戳和采样帧位置映射，不再用回调到达时间或无依据的全局补偿估算录音时刻；使用 `MIC` 输入恢复正常录音响度。
- Xiaomi 17 Pro 验证表明，抽取出的原始 MP4 在电脑剪辑软件中音画同步，而小米图库的 Motion Photo 播放链路会额外产生约320ms声音延迟。最终封装仅在 Xiaomi 设备上将音频内容提前320ms，并多保留同等长度的尾部录音；用户真机复测已基本对齐。
- 新增音频硬件时间映射和移位音频窗口测试；单元测试、Lint 与 Debug 构建通过。

## 0.6.0

- Live 拍摄接入公开 Camera2＋MediaCodec 后端：持续预览、4096×3072独立 JPEG、4:3 H.264 环形缓冲和 AAC 音频可同时工作，普通照片与独立录像继续使用 CameraX。
- 每张已接受快门按 JPEG 传感器时间导出独立 Motion Photo，支持约每秒2张连续5张；重叠任务共享不可变编码样本，保存与编码并行。
- 默认保留最近约4秒编码数据，活动任务上限8个、编码预算64MiB；队列满时禁用快门并显示数量，容量释放后自动恢复。
- 麦克风未授权或无法可靠映射时间戳时生成无声 Live；动态导出失败时保留普通照片，不输出已知错位或损坏的动态文件。
- 切镜头、关闭 Live 和启动独立录像时收束已有任务：只等待已提交 JPEG 最多1秒，并用已有尾段导出缩短片段。
- Room 数据库升级到版本2，照片、Live 和独立录像统一使用持久保存队列；写入前记录目标 URI，SAF 串行保存并完整读回校验，旧 DataStore 恢复项幂等迁移。
- 重启恢复会先验证临时 JPEG、Motion Photo 或 MP4 材料；无法恢复的记录明确标记失败且不占拍摄容量，用户可确认后清除失败材料。
- 增加结构化采集日志、时间窗/缓冲/迁移测试、Motion Photo 解码验证和音画同步分析工具。
- 目标小米手机主摄1×明亮场景完成10组共50张连续有声 Live，50/50保存并解码成功、曝光均低于200ms；音画冲击点差10.855ms，10分钟预录后仍可连续拍摄。

## 0.5.4

- 修复录像结束后照片误用9:16裁切：拍照与Live绑定固定使用竖向3:4 ViewPort，不再读取录像退出动画期间的取景尺寸。
- 相机页音量加、减键均可触发快门，遵循普通照片/Live设置；录像或暂停时按音量键结束录像。长按不重复触发，保存期间不重复拍摄，离开相机页恢复音量键原用途。

## 0.5.3

- 对焦框右侧曝光条的完整曝光范围手势距离由约240dp调整为约300dp；80dp显示长度保持不变，降低误触和过量调节。
- 录像底栏固定预留156dp，不再依赖16:9取景后偶然剩余的空间；取景保持16:9并居中，控制按钮不会跨入亮区。
- 暂停和翻转按钮恢复为52dp圆形按钮、28dp内部图标，停止按钮调整为72dp，并统一三列居中关系和录像状态上下间距。

## 0.5.2

- 设置菜单新增默认开启且跨重启保留的“同步防抖”开关，仅作用于长按快门录制的独立视频。
- 独立视频的 Preview 与 VideoCapture 同时启用 CameraX 防抖、保持16:9并统一请求30fps；生效时在录像计时旁显示黄色稳定器图标。
- 开始录像前同时检查当前镜头的防抖能力和30fps范围；不支持时保持普通录像并明确提示，不修改用户偏好。
- 设备宣称支持但稳定组合绑定失败时，自动以无防抖30fps组合重试一次；实际状态区分启用、用户关闭、不支持和绑定降级，避免假报生效。
- 防抖录像中只允许切换到同样支持同步防抖和30fps的镜头；目标绑定失败时恢复原镜头，继续复用同一MP4。
- 新增同步防抖策略单元测试、领域术语与 ADR 0008；60fps同步防抖列入暂缓清单。

## 0.5.1

- 录像控制区固定在取景亮区下方的黑色面板内并裁剪越界内容，顶部增加18dp间距；暂停和翻转按钮缩小为40dp、图标缩小为21dp，停止按钮缩小为68dp。
- 独立录像改为 CameraX 持久在线录像；录像中切换前后摄时复用同一个 `VideoCapture` 和输出文件，不以停止后新建视频冒充无缝翻转。
- 录像计时旁清晰度文字由“1080p优先”精简为“1080p”。
- 对焦框右侧曝光条显示长度保持80dp，拖动灵敏度降为原来的三分之一：跨越完整曝光范围需要约240dp手指位移。
- 防抖能力留待下一小版本，在设备能力、预览一致性和视野裁切策略确认后实施。

## 0.5.0

- 长按快门进入独立 16:9 视频录制；短按继续按当前设置拍摄普通照片或 Live 图。
- 首次录像单独申请麦克风权限，拒绝时不录制无声视频；授权成功后需再次长按，避免误启动。
- 录像优先 1080p、设备不支持时由 CameraX 回退 720p，支持暂停、继续、计时和红色停止键。
- 录像期间冻结分类、根目录、镜头、方向、设置和 Live 状态，保留缩放、对焦与曝光调节。
- 单段最长 10 分钟，9分30秒提示；按可用缓存预留 `max(256 MiB, 10%)`，安全录像预算不足 128 MiB 时拒绝开始。
- 切后台或离开相机页时自动停止并收尾，MP4 通过视频轨/时长检查及 SAF 逐字节读回后才报告保存成功。
- 视频按 `VID_yyyyMMdd_HHmmss_SSS.mp4` 命名并保存到当前分类目录；失败保留临时文件及持久化重试入口。
- 照片、Live 图与视频统一到 `MediaStorage` 保存契约，待恢复记录新增媒体类型。

## 0.4.4

- 根据 Xiaomi 17 Pro 真机结果暂时搁置背屏预览，完全移除相机页背屏按钮、状态和 `Presentation` 镜像实现。
- 移除背屏相关运行时类和 CameraController 接口，不再枚举或请求任何副屏窗口。
- README 新增“暂时搁置的问题”，记录背屏、系统相机级连续 Live 拍摄、厂商图库 Live 方向与编辑兼容的原因和重启条件。

## 0.4.3

- 根据 Xiaomi 17 Pro 真机的 `permission denied for window type 2037` 确认：HyperOS 虽可枚举背屏，但未授权普通第三方应用创建 Presentation 窗口。
- 修正背屏筛选：无论显示来自 Presentation 分类还是全量列表，都只尝试有效、公开且带 `FLAG_PRESENTATION` 的非默认显示，不再误用私有显示。
- 捕获 `BadTokenException`、`InvalidDisplayException` 和 `SecurityException`；系统拒绝后停止本次相机会话内的重复尝试、将背屏按钮置灰，并显示明确中文能力边界。
- 不新增悬浮窗或厂商私有权限；该权限不能授权应用访问 HyperOS 私有背屏。

## 0.4.2

- 使用用户提供的系统相机原始动态照片与 Lenshelf 原片进行二进制对照：确认 Lenshelf 文件末尾 MP4 完整、长度和 `ftyp` 起点一致，问题不在保存或附件传输。
- XMP 相机命名空间前缀改为系统原片同款 `GCamera`，Container目录项改为嵌套 `<Container:Item>`；解决 JarkViewer v1.34 通过 Exiv2 键名字符串匹配时无法取得视频长度的问题。
- Live静态照片由点击后约0.9秒改为按下快门后立即发起 ImageCapture；对应视频时间改为从录制开始时刻计算，录制尚未开始时使用0微秒。
- 小米相册内播放翻转与原始文件方向区分处理：本版不再增加厂商专用旋转修正，等待通用播放兼容方案。
- Live 视频 Recorder 明确请求 4:3 画幅；Preview与ImageCapture共用4:3 ResolutionSelector，三者绑定在同一ViewPort；设备无法同时提供该组合时继续安全回退普通照片。
- Live 拍摄开始时冻结 JPEG 与 MP4 的同一目标方向，封装完成后再恢复方向监听，避免录制期间姿态变化造成两部分方向不一致。
- 关键照片时间由固定 0.9 秒改为依据 CameraX RecordingStats 和单调时钟推算，写入现代与旧版兼容的 presentation timestamp。
- Live 文件名改为 `IMG_yyyyMMdd_HHmmss_SSS_MP.jpg`，符合 Android Motion Photo 规范建议；失败重试继续保留 `_MP` 身份。
- XMP 同时写入 Motion Photo 1.0 的 Container/Item 字段和只用于旧读取器兼容的 MicroVideoOffset 字段，并补充标准 XMP packet 包装。
- 分析 JarkViewer 开源检测逻辑：它优先读取 `Xmp.GCamera.MicroVideoOffset`，旧版或 Exiv2 未正确展开现代 Container 字段时会把 Lenshelf 文件当静态 JPEG；本版通过双字段兼容修复。

## 0.4.1

- 修复开启 Live 图后 CameraX 预览层覆盖顶部工具栏、导致图标不可见但仍可点击的问题：工具栏明确置于上层，4:3 取景区裁剪自身绘制范围。
- 根据 Xiaomi 17 Pro 录屏确认，0.4.0 生成文件的 JPEG 与 MP4 存在方向、画幅和帧同步不一致，播放与缩放会出现横屏和碎片化画面。
- 保留 0.4.0 的通用 Motion Photo 实验拍摄方式和持久化开关，不接入小米或其他厂商专用 SDK；普通 JPEG 拍摄路径保持不变。
- 明确能力边界：Android 当前只有通用 Motion Photo 文件规范，没有可嵌入自有相机界面的通用动态照片拍摄 SDK；Android 16 的标准动态照片 Intent 会调用系统相机。
- Live 图仍需等待约 2.1 秒且暂不支持连拍，已知的图库方向和碎片化兼容问题留待后续通用方案修正。

## 0.4.0

- 在后摄取景区左下方新增实验性背屏预览按钮：后摄可点击、启用后变为黄色，切换前摄时自动关闭并变灰。
- 背屏预览使用 Android 标准副屏探测与 `Presentation`，以 8fps 镜像主取景画面；系统未向第三方应用开放背屏时给出明确提示，不调用私有权限。
- 相机设置新增持久化的 Live 图开关；首版生成约 2.1 秒、静态帧位于约 0.9 秒处的无声 Motion Photo 单文件。
- Live 图使用 CameraX `VideoCapture + ImageCapture`，并按 Android Motion Photo 1.0 写入 XMP、在 JPEG 末尾附加 MP4；当前镜头不支持该组合时自动关闭并回退普通拍照。
- Live 图继续沿用原有两阶段 SAF 校验保存和失败重试，不新增麦克风、媒体读取或全盘存储权限。

## 0.3.5

- 底部曝光控件相对 0.3.4 的位置向下偏移 8dp，便于后续按明确数值继续微调。
- 底部曝光控件静止时以 28% 不透明度弱化显示，开始拖动后在 160ms 内恢复至 100%，拖动结束后重新淡出。
- 对焦框旁曝光条由 240dp 缩短为三分之一，即 80dp，宽度继续保持 28dp。

## 0.3.4

- 修复切换前后摄后倍率快捷栏仍保留旧镜头状态的问题；切换时移除旧镜头监听，并立即读取新镜头的实际倍率和可用范围。
- 底部曝光控件改为更短的半透明胶囊和细轨道，减少视觉占用。
- 对焦框旁曝光条与底部统一为细轨道风格，长度增加为原来的两倍、宽度进一步收窄，两处继续保持实时同步。

## 0.3.3

- 修复相机内切换分类时销毁、重建 CameraX 导致部分设备取景卡住的问题；现在保持同一个相机实例，仅更新当前保存分类。
- 保存过程中禁止切换分类，避免拍摄回调把照片保存到错误目录。
- 顶部分类胶囊缩短并居中，名称最多显示 5 个字符，完整名称仍保留在下拉列表中。
- 倍率栏最左侧新增实时倍率；快捷倍率使用 260ms 平滑变焦，选中背景和尺寸增加 220ms 过渡动画。
- 曝光调节改为底部横向 Slider；点按对焦后在对焦框旁显示竖向 Slider，两处读取和更新同一个 CameraX 曝光索引。
- 对焦提示停留时间由 1.2 秒延长为 4 秒，拖动竖向曝光条时重新计时。

## 0.3.2

- 修正左右横握时倍率文字的旋转方向，仅倍率快捷文字随方向变化，并加入 320ms 平滑旋转动画。
- 方向识别增加 55° 退出迟滞、28° 进入范围和 550ms 稳定时间，减少轻微晃动造成的反复切换。
- 闪光灯移入相机设置菜单，顶部改为紧凑的“分类选择 + 文件夹”组合胶囊。
- Preview 与 ImageCapture 明确使用 4:3，相机亮区固定为纵向 3:4，并继续共用 ViewPort 保持取景与成片范围一致。
- 顶部工具栏移出取景亮区；倍率快捷栏改为半透明悬浮在取景区底部，快门和小型曝光控制保留在下方黑色区域。

## 0.3.1

- 取景区域与底部控制区改为独立布局，不再用半透明面板遮住预览；曝光控制缩小并移动到底部。
- CameraX 预览与 ImageCapture 绑定同一个 `UseCaseGroup + ViewPort`，使 JPEG 裁切范围与明亮取景区域保持一致。
- 删除不常用的 `10×` 快捷倍率，仍可在硬件允许范围内双指缩放。
- 拍照页固定原有竖屏布局，方向传感器只旋转文字和图标，并同步 JPEG 输出方向。
- 顶部新增相机设置入口，可开启或关闭持久保存的井字格参考线。

## 0.3.0

- 参考系统相机重新组织拍摄页：分类与文件夹入口置顶，闪光灯置于顶部，倍率、曝光、快门和翻转集中到底部控制区。
- 当前分类旁新增文件夹按钮，复用首页的通用/小米兼容逻辑打开对应目录。
- 新增按相机实际最小、最大缩放范围筛选的 `0.5× / 0.7× / 1× / 2× / 5× / 10×` 预设倍率，并与双指缩放状态同步。
- 点按取景画面时显示黄色对焦标识；双指缩放结束不再误触发对焦。
- 拍摄页支持全传感器方向自动旋转，并把当前显示方向传给 CameraX 预览与 JPEG 输出。

## 0.2.3

- 分类管理卡片改为紧凑单行布局，上移、下移继续作为快捷操作。
- 停用、恢复和删除分类收纳到每项右侧的三点菜单，不再由“更多操作”额外占据一行。
- 相机顶部操作栏接入系统安全绘制区域，自动避开状态栏、刘海与挖孔区域。

## 0.2.2

- “管理分类”中新增折叠的危险操作区域和删除分类功能。
- 删除时可以同时删除对应的空文件夹；文件夹内存在任何项目时拒绝整个删除操作，避免误删照片。
- 有待恢复照片的分类禁止删除；应用始终至少保留一个分类。
- 默认继续使用 Android 通用目录打开方式，不改变其他品牌设备的行为。
- Xiaomi、Redmi 和 POCO 设备的设置页增加可选“小米文件管理兼容”开关，默认关闭。
- 兼容模式把主存储 SAF 路径转换为真实目录路径，通过小米 `miui.intent.action.OPEN` 与 `explorer_path` 尝试直接定位分类目录；不兼容时回退到 Android 通用方式。
- 修复错误转授权小米自有目录 URI 导致兼容启动被系统拒绝、随后回退到打开方式选择框的问题。
- 修复普通 `ACTION_VIEW` 只能启动小米文件管理首页、目录参数被忽略的问题。
- 修复首页重复处理状态栏安全区造成的顶部大块空白。
- 首页改为双色 Lenshelf 字标，标题、副标题和设置入口贴合顶部布局。
- “选择一个分类”提示卡默认折叠并支持展开，“管理分类”按钮固定在页面底部。

## 0.2.1

- 应用正式命名为 **Lenshelf**，版本号更新为 0.2.1。
- 启动图标主体缩放为上一版的 80%，保留安全留白。
- 重做首页、首次设置页、分类管理页和设置页的视觉层级与间距。
- 每个启用分类新增“文件”按钮，可尝试直接在文件管理器中打开对应目录。
- 当手机文件管理器不支持目录 URI 跳转时，显示明确提示。

## 0.2.0

- 新增候选品牌图标，融合文件夹与相机镜头。
- 全局配色调整为 Ink Navy、Clear Blue 与 Mist Gray。
- 首页分类卡改为低对比白色卡片与蓝色功能图标。
- 增加英文名候选与阶段性版本规划。

## 0.1.1

- 修复部分手机 SAF 元数据导致照片写入后被误判失败的问题。
- 写入验证改为源文件与目标 URI 逐字节比对。

## 0.1.0

- 完成 Android 13+ 首个可安装 MVP。
- 支持选择并持久授权照片根目录，自动创建 `.nomedia` 与分类子目录。
- 支持默认分类、新建、排序、停用和恢复。
- 支持 CameraX 拍照、前后摄切换、闪光灯、缩放、点按对焦和曝光补偿。
- 支持临时照片写入、目标目录保存校验及失败重试。
